# Phase B5 報告: 実機調整・品質確定

対象指示書: `phase_b5.md`。**本フェーズは実機での官能確認 (映像・音・
操作感) がユーザ作業なので、実装担当が終えられる範囲 (ビルド・計測・
文書) をここに記録し、ユーザ確認待ちの項目を明示する。**

## 1. T5-1 / T5-5: 実機ビルド

両ターゲットとも `rake clean_all` からのフルビルドが通ることを確認した。

| ターゲット | 結果 | バイナリ | 空き |
|---|---|---|---|
| Retro (NARYAv3 / ESP32-S3) | 成功 | 2,107,248 バイト (0x202770) | パーティション 0x300000 中 33% |
| Modern (TAB5 / ESP32-P4) | 成功 | 0x3798F0 バイト | パーティション 0x600000 中 42% |

B4.5 で入れた 2bpp 化と実キャラクタの絵柄を含んだ状態。P4 は表示経路
(display_p4) と音声経路 (apu_emu ローカル) が S3 と違うが、extension 層は
`fmrb_gfx` / `audio_commands` の共通 API しか使っていないので分岐漏れは
出ていない (ビルド時の未定義参照ゼロ)。

**フラッシュとリセットはユーザ操作**。Tab5 は DTR/RTS リセット非対応なので、
書き込み後は本体のボタンでリセットが必要。

## 2. T5-2: 速度計測

### 計測の仕組み

- ベンチは `components/basic/test/samples/bench_0*.bas` の 5 本。
  開始・終了マーカーを PRINT し、**ログのタイムスタンプ**から所要時間を取る。
  背景アプリ (画面なし) として起動するので PRINT がログに出る。
- ハーネスは `tool/basic/basic_bench.rb`。シミュレーションに対しては
  debugd 経由で起動し `docker logs` を読む。**実機では
  `--log <シリアルログ>` で同じ集計ができる**ので、ユーザが取った
  ログをそのまま渡せる。
- 素の実行速度を測るために **`_SPEED n`** を追加した (1 フレームあたりの
  実行文数、`0` で無制限)。`_` 始まりなので既存プログラムと衝突しない。
  これが B3 で入れたペーシング (60 文/フレーム) の校正ノブそのもの。

### Linux シミュレーションの計測値 (基準)

x86 のネイティブビルドなので**実機の予測値ではない**。ハーネスの動作確認と、
実機値と並べたときの上限の目安として記録する。

| ベンチ | 内容 | 所要 | 速度 |
|---|---|---|---|
| `bench_01_loop` | 空の FOR/NEXT 100,200 文 | 0.008 s | 12,525,000 文/s |
| `bench_02_expr` | 式評価 80,000 文 | 0.022 s | 3,636,364 文/s |
| `bench_03_string` | 文字列操作 40,000 文 | 0.020 s | 2,000,000 文/s |
| `bench_04_tick` | `PAUSE 600` (= 10 秒のはず) | **10.001 s** | 誤差 **+0.0%** |
| `bench_05_paced` | ペーシング有効の同じループ 2,040 文 | 0.517 s | **3,946 文/s** |

読み方:

- **フレームクロックは正確** (10 秒に対し +1 ms、欠落なし)。PAUSE と
  スプライト自動移動が同じクロックに乗っているので、ここが合っていることが
  ゲームのタイミングの土台になる。
- **ペーシング実効 3,946 文/s** は設計値 (60 文 x 60 フレーム = 3,600 文/s) と
  同じ桁。差はフレーム境界の刻み方によるもので、意図どおり効いている。
- 素の速度 (bench_01-03) はペーシングの 3,000 倍以上あり、シミュレーションでは
  ペーシングが完全にボトルネック。**実機で同じ関係になるかが T5-2 の焦点**で、
  ここが逆転する (素の速度がペーシング目標に届かない) なら、ノブを下げるか
  該当処理を速くする必要がある。

### 実機計測の手順 (ユーザ依頼)

ベンチはソースツリー (`components/basic/test/samples/`) にしか無いので、
そのままではデバイス上のエディタから開けない。**`rake basic:bench` で
`flash/home/bench/` へ入れてから焼く**。

1. `rake basic:bench` (ベンチ 5 本 + .toml を flash へ配置)
2. `rake build:esp32` して書き込み、リセット
3. シリアルログを取りながら、エディタで `/home/bench/bench_01_loop.bas` を
   開いて **F5**。5 本繰り返す
4. ログをファイルに保存して `ruby tool/basic/basic_bench.rb --log そのファイル`

`.toml` は **背景アプリ (画面なし)** として起動するよう書かれている。
画面を持つアプリの PRINT はログに出さない仕様なので (B5 のログ整理)、
マーカーをログで拾うにはこれが必要になる。

## 3. T5-3 / T5-4: 音と入力

### 計測で確認できたこと

- **テンポ精度**: B3 の時点で FMSQ を WAV に落として実測済み。
  `T4` の 4 分音符 = 0.500 秒ちょうど、`O3` の C = 261.35 Hz (C4 -1.8 cent)。
  変換は決定的なのでゴールデンでバイト列を固定してある。
- **フレームクロック**: 上記 `bench_04_tick` のとおり誤差 +0.0%。
  PLAY の進行もこのクロックに乗る。

### ユーザ官能確認をお願いしたいこと

| 項目 | 確認内容 |
|---|---|
| PLAY / BEEP (S3 / WROVER APU) | 3 声が鳴るか、テンポ・音程が実機 Family BASIC と比べて違和感がないか |
| PLAY (P4 / TAB5 ローカル apu_emu) | 同じプログラムが鳴るか |
| NTSC 出力 | 224x192 のにじみ・オーバースキャン。枠色や配置の調整が要るか |
| USB ゲームパッド | STICK / STRIG の同時押し、抜き差し、未接続時にエラーにならないこと |
| カナ入力 | TAB トグル + ローマ字 2 打が実機のキーボードで打てるか |

`sample_11_shoot` (発射音 + BGM + パッド) と `sample_13_music` (3 声) が
確認用として手頃。

## 4. T5-6: 文書

| 文書 | 内容 |
|---|---|
| `doc/fmrb_basic/usage.md` | **新規**。プログラムの置き方と F5、キーボード/パッド対応表、カナ入力、独自拡張 (CIRCLE / PRESENT / _SCRDUMP / _SPEED)、SAVE と LOAD の扱い、MML、絵柄シートの差し替え、困ったときの見どころ |
| `doc/fmrb_basic/compat_list.md` | **新規**。自作サンプル 6 本の互換状況。ターゲット作品は**リスト提供待ちで未着手**。非対応/制限付きになりやすい条件の判定目安を書いた |
| `doc/fmrb_basic/known_differences.md` | B4.5 で作成済み。疑義全件を 4 区分に割り当て済み |
| `doc/fmrb_basic/virtual_memory_map.md` | B4 で作成済み。POKE/PEEK 対応表 |

spec の資料待ち項目は `known_differences.md` の区分 3 に集約済みで、
実機映像・録音が手に入ったときに差し替える箇所 (ファイルと表名) を
各行に書いてある。**実機挙動と照合できたものが出たら spec と区分 3 を
同時に更新する**のが以後の手順。

## 5. 検証

| 項目 | 結果 |
|---|---|
| `rake basic:test` | **103 passed, 0 failed** |
| `tool/basic/basic_screen_check.py` (linux sim) | **15 passed, 0 failed** |
| `rake build:esp32` (NARYAv3) | 成功 |
| `FMRB_HW_TARGET=TAB5 rake build:esp32` | 成功 |
| `rake build:linux` | 成功 |
| 実機動作 | **ユーザ確認待ち** |

## 6. 受け入れ基準の対応

| # | 基準 | 状況 |
|---|---|---|
| 1 | S3 実機でターゲット作品が動作リストどおり | **保留**。作品リスト未提供 + 実機確認はユーザ作業 |
| 2 | TAB5 で代表サンプルが動く | **保留** (ビルドは通っている) |
| 3 | ベンチ計測値と tick 精度が report にある | **達成** (sec 2。実機値は計測手順つきで依頼) |
| 4 | compat_list.md / usage.md がある | **達成** |
| 5 | 全ゴールデン green | **達成** (103 + sim 15) |
| 6 | 本レポート | 本ファイル |

## 7. プロジェクト総括

### やったこと

| フェーズ | 内容 |
|---|---|
| B0 | 仕様抽出、コーパス、ゴールデンテスト機構 |
| B1 | 言語コア (クランチ格納、明示スタック評価器、31 字文字列) |
| B2 | テキスト画面 28x24、自作フォント、カナ入力 |
| B3 | スプライト / DEF MOVE / PLAY (MML->FMSQ) / STICK・STRIG |
| B3.5 | kill の協調終了化、フォーカス、エディタ RUN (F5) |
| B4 | ON ERROR、仮想 POKE/PEEK、LOAD/SAVE、画面系、Ctrl-Q、配列追従 |
| B4.5 | FILTER 着色、2bpp 多色化、LIST 忠実化、既知差異の確定 |
| B5 | 実機ビルド、速度計測の仕組み、利用者文書 |

ゴールデンテストは **62 (B1) -> 75 (B2) -> 86 (B3) -> 102 (B4) -> 103 (B4.5)**、
シミュレーション画面テストは **11 -> 15**。

### 残課題

すべて `known_differences.md` に区分つきで載っている。要約:

- **区分 3 (データ差し替え待ち)** が実質的な残作業。実配色、52 色表、
  MML の絶対テンポ/オクターブ、`$D000` の原点、文字コード表の未確定分。
  いずれもコードは完成しており、**表の値を書き換えるだけ**。
- **区分 4 (対象外)** は実装しない。マシン語 CALL、直接モードコンソール、
  LOAD/LOAD?、BACKUP、VIEW、BGTOOL、テープ入出力、命令単位のタイミング互換。
- ターゲット作品リストが未提供なので `compat_list.md` の本体が空。

### 以後のメンテナンス方針

1. **コアを触ったら `rake basic:test`**。ホスト g++ だけで走り、docker も
   実機も要らない。数秒で終わる。
2. **画面・スプライト・入力を触ったら `tool/basic/basic_screen_check.py`**。
   シミュレーションを立てて (`tools/dev_run_check.sh --keep`) 実際の描画経路を
   通す。ホストランナーでは通らない経路の回帰はここで捕まる。
3. **速度に関わる変更は `tool/basic/basic_bench.rb`** で before / after を取る。
   **同一コミット同士で比較する** (別日の記録と突き合わせない)。
4. 仕様の疑義が出たら spec を直す前に report に疑義として記録し、
   決着したら `known_differences.md` の区分へ移す。区分の無い疑義を
   残さないことが、この文書群の価値を保つ条件。
5. 絵柄を変えるときは `flash/usr/share/basic/*.bmp` を差し替える。
   生成器 (`tool/basic/gen_basic_*.rb`) は初期値を作るためのもので、
   手で描いたシートを上書きしないよう `rake basic:sheets` は既存ファイルを
   守る (`FORCE=1` が要る)。
