# P1 実施報告: エディタ本気モード (全画面 + 計測 + 差分描画 + sim 解像度連動)

指示書: ../instruction_p1.md / 計画: ../plan.md

## T1: エディタ全画面「本気モード」

### 実施内容

- spawner に組み込みエントリ `default/editor_fs` を追加
  (main/app/fmrb_app_spawner.c)。irep は `default/editor` と同じ
  editor_irep。属性は fullscreen = true / fullscreen_switchable = true /
  rounded_corners = false (全画面は枠を描かないので不透明 canvas にして
  合成を軽くする) / large_memory なし。
  アプリ名は窓版と同じ `FM-Editor` にした。shell と desktop の
  「エディタを名前で探してファイルパスを渡す」中継がそのまま効くため。
- 全画面組み込みエントリの窓サイズは spawn_builtin_app で display 設定から
  埋める。**指示書の想定 (fmrb_app.c:1512-1516 が display サイズを入れる)
  は SYSTEM_APP の分岐**で、USER_APP は attr の値が 0 だと
  default_user_app_width にフォールバックしてしまう。そのため toml 経路
  (同ファイルの fullscreen 分岐) と同じ計算をここでも行う。
- 入り口: shell の `edit` に `-f` を追加 (`edit [-f] <file>`)。
- エディタに `on_resume` を追加し、park 復帰時に全面再描画する。

### 想定外だった点: ファイルパスの中継が全画面では成立しない

指示書は「既存の shell → editor 中継 (tick_pending_edit) をそのまま使う」
としていたが、**全画面ではこの経路は原理的に動かない**:

1. shell が kernel に spawn 要求を出す。
2. kernel が spawn 直後に `enter_fullscreen` で **shell を suspend** する。
3. shell の tick_pending_edit (3 tick 後にパスを送る) は永久に来ない。

そこで kernel の spawn 要求に任意フィールド `open_path` を足し、
**kernel 自身**が新しい pid に `file_selected` を送る形にした
(kernel は suspend されない)。窓モードは従来の中継のまま (退行なし)。

さらに、spawn 直後の送信は落ちる:

```
W fmrb_msg: msg_send DROPPED: dest=5 queue not registered
```

アプリのメッセージキューは**アプリ自身のタスクが起動時に登録**するため、
spawn 直後にはまだ無い。よって kernel の tick_process で
`_try_send_raw_message` を成功するまで再試行する (上限 60 tick)。
実装は fmrb_kernel/app_lifecycle.rb の `flush_open_path`。

### 検証 (Linux sim, 320x240, kernel=spinel)

すべて headless (tools/dev_run_check.sh + fmrb_input.rb + fmrb_screenshot.py)。

| 確認 | 結果 | 画面 |
|---|---|---|
| `edit -f /home/test/hl_small.rb` で全画面起動、枠なし、メニュー〜ステータス行が画面全体 | OK (52 桁 x 27 行) | t1_fs2.png |
| 打鍵 → 反映、Ctrl+S 保存 ("Saved" バッジ) | OK | t1_edit_save.png |
| ハイライト表示 (全画面) | OK | t1_fs2.png |
| Ctrl+Tab で park → デスクトップ/shell が復帰 | OK | t1_park.png |
| Ctrl+Tab で復帰 → 全画面に戻り内容も維持 | OK | t1_unpark.png |
| 窓モード `edit <file>` の従来動作 | OK (退行なし) | t1_window4.png |

画面は commit には含めていない (作業ディレクトリのスクラッチ)。再現手順は
上記コマンド列のとおり。

- esp32 ビルド: S3 (NARYAv3) / P4 (TAB5) 両方成功。
- **ユーザ確認待ち**: 実機 (Retro / Modern) での見た目と操作感。

### 副次的にわかったこと

- 全画面エディタを Ctrl+X で閉じると HID フォーカスは**デスクトップ**に
  戻る (shell ではない)。デスクトップにフォーカスがある状態で `e` を押すと
  system_conf の shortcuts で editor が起動するので、続けて文字を打つと
  「新しいエディタに打ち込まれる」ことになる。既存挙動。
- 実行時の窓 ⇔ 全画面トグルは指示書どおり未実施 (P4 の canvas が spawn 時
  サイズ固定)。Retro 側の可否は plan.md 3 章の記述のまま (未実測)。

### T1 で見つけて直したバグ: 全画面アプリの上端 13px にクリックが届かない

全画面エディタのメニューバー (File/Edit/...) をクリックしても何も起きない。
原因は kernel の入力経路 2 箇所が、**画面上端 13px (デスクトップの
メニューバー行) を無条件でデスクトップへ回していた**こと:

- input_router.rb: `if @fullscreen_pid && y >= $menu_bar_height` — 全画面でも
  上端だけは除外していた (コメントに明示されていた設計)。
- window_manager.rb `find_window_at`: `y < $menu_bar_height` なら
  デスクトップの窓を返す。

全画面中はデスクトップは suspend されメニューバーも画面に出ていないので、
上端もそのアプリのものにした (park 中は @fullscreen_pid が nil なので
デスクトップにメニューバーが戻る)。修正後、全画面でも Hilight / File
メニューのクリックが効くことを確認。

## T2: 計測の整備

### 何を測れるようにしたか

1. **打鍵 → present (エディタ Ruby 側)**: editor.app.rb に常時オンの計測を
   追加。キー到着で `Machine.uptime_us` を 1 回、redraw 前後で 1 回ずつ。
   累積は整数のみ、100 サンプルごとに 1 行だけログを出す:

   ```
   edit_lat: n=100 mean=29051us max=68397us p99<=55ms over25ms=49
             draw_mean=2509us draw_max=7082us hist5ms=4,8,13,... rows=27 hl=1
   ```

   - サンプルは「まだ画面に出ていない最古のキー → present」。連続打鍵で
     甘い値が出ないようにこちら側に寄せてある。
   - p99 は 5ms 刻みのヒストグラムからの上限見積り (サンプルを保存しない)。
   - `draw_*` は redraw_all の所要時間。**present は非同期** (gfx コマンドを
     host_task のキューに積むだけ) なので、これは「Ruby が描画コマンドを
     作り終えるまで」であって画面に出るまでではない。画面側は下の 2。
   - 修飾キー単独 (keycode 224-231) は計測開始にしない。

2. **1 フレームの描画コスト (画面側)**:
   - Retro / sim: graphics-audio の graphics_task に既にあった
     `loop: fps=... render_avg=...` を**マイクロ秒化**した。従来は
     FreeRTOS tick (Linux ビルドは 10ms) で割っていたため render_avg=0ms
     しか出ず使えなかった。合成 (render) とパネル/SHM への転送 (disp) を
     分けて出す:

     ```
     loop: fps=30.3 render_avg=35us render_max=100us disp_avg=17us disp_max=90us late=0
     ```

     同じコードが WROVER (Retro の映像側) でも動くので S3 構成でも取れる。
   - Modern (P4): display_p4_task に既に
     `render: N frames/5s avg=..ms max=..ms` があるので追加なし。

### T1 時点の基準値 (Linux sim 320x240, kernel=spinel, 差分描画なし)

打鍵は 70ms 間隔で 110 文字 (実際の打鍵速度相当) を注入。100 サンプルの集計。

| 条件 | 行数 | HL | mean | max | p99 | 25ms 超 | draw mean | draw max |
|---|---|---|---|---|---|---|---|---|
| A 全画面 / 小 (384B) | 27 | on | 29.1ms | 68.4ms | <=55ms | 49/100 | 2.51ms | 7.1ms |
| B 全画面 / 大 (10.9KB) | 27 | 自動 off | 28.4ms | 66.0ms | <=55ms | 53/100 | 2.37ms | 7.7ms |
| D 窓 / 小 (384B) | 21 | on | 29.0ms | 67.1ms | <=55ms | 54/100 | 2.03ms | 7.2ms |
| E 窓 / 大 (10.9KB) | 21 | 自動 off | 29.2ms | 69.8ms | <=55ms | 52/100 | 2.71ms | 8.2ms |

画面側 (GA): 待機時 render_avg 25-57us、打鍵中 34-35us、max 0.5ms 程度。
sim では画面合成は遅延要因になっていない。

### この数字が示していること (T4 の方針に直結)

**遅延の主因は描画ではなくポーリング 2 段**である。全面再描画のコストは
2.0-2.7ms しかないのに、打鍵→present は平均 29ms、最大 68ms:

1. キーは on_event で `@input_buffer` に積まれるだけ。
2. `@editor_task` (Task.new) が `getch` で `sleep_ms 33` しているので、
   最悪 33ms 待ってから handle_key が走り `@need_redraw` が立つ。
3. 実際の描画は次の `on_update` (これも 33ms 周期) で行われる。

33ms x 2 = 最悪 66ms。実測 max 68ms とほぼ一致する。よって
**dirty-line 差分描画だけでは T4 の目標 (p99 < 33ms、25ms 超ゼロ) には
到達しない**。T4 では入力経路も直す (キーを on_event で直接処理し、
その場で差分描画する)。副作用として Task.new + sleep_ms が消えるので、
plan.md 6 章の Spinel 化の障害 1 も同時に解消する。

窓 vs 全画面の差は sim ではほぼ無い (CPU が余っているので suspend の効果が
出ない)。全画面の方が行数が多い分 draw が僅かに重い。実機 (特に S3) での
比較は**ユーザ確認待ち**。

### 併せて見つかった既存バグ: 10KB ファイルでハイライトを ON にすると固まる

10.9KB / 719 行のファイルを開き、Hilight を ON にすると**エディタが停止する**
(窓モード / 全画面モードの両方で再現。present が一切出なくなり、以後キー
イベントはアプリのキューが埋まって捨てられる)。gdb で見ると当該 VM の
スレッドは `mrb_hal_task_idle_cpu` で待っており、**mruby-task に実行可能な
タスクが 1 つも無い**状態。picoruby の既知の落とし穴 (`sleep_ms` は
`_spin` の外では tick が進まず止まる) と同じ症状。

- HL_AUTO_LIMIT_BYTES = 1024 があるため既定では踏まない (手で ON に
  した時だけ)。T1 の変更とは無関係 (窓モードでも起きる)。
- 引き金は「全文 join + 全画面分の色分割 draw_text」の重い redraw。
  T4 (行単位の増分ハイライト + 差分描画 + 入力経路の直し) で引き金が
  無くなる見込み。**T4 後に同じ手順で再確認する**。

## T3: Linux sim の解像度を .env のターゲットに連動

### 手検証の結果 (指示書の「先に手検証」)

config/system_conf_linux.toml を 426x240 に手で書き換えて起動:

- **1 回目の起動は 320x240 のまま**。graphics-audio は解像度を
  flash/etc/display_conf_linux.txt に保存しており、INIT_DISPLAY で違う値を
  受け取ると `Display config mismatch!` を出して**保存だけして次回に回す**。
  2 回目の起動で sdl2-display も 426x240 (窓 852x480) で上がる。
- fmrb_screenshot.py は 426x240 の PNG を出す (SHM ヘッダのサイズに追随)。
- fmrb_input.rb の座標も追随。x=352 (旧画面の外) のランチャーのスクロール
  矢印クリックが効くことで確認した (t3_scroll.png)。
- sdl2-display / 検証ツール側の固定値は無く、**変更不要**だった。

### 実装

- config/system_conf_linux_p4.toml を追加 (426x240、default_user_app_width
  386。それ以外は system_conf_linux.toml と同じ = sdl2 のまま)。
- Rakefile の build:linux で HW_TARGET が MODERN_HW_TARGETS (TAB5 /
  NARYAv4) ならそちらをコピーする。
- ルート CLAUDE.md の「座標はフレームバッファ座標 (320x240)」に
  ターゲット連動と「解像度変更は次回起動から」の注記を足した。

### 受け入れ確認

- `FMRB_HW_TARGET=TAB5`(.env 既定) → `rake build:linux` →
  dev_run_check.sh で **426x240** の PNG (t3_final.png)。
- `FMRB_HW_TARGET=NARYAv3 rake build:linux` → flash/etc/system_conf.toml が
  320x240 (従来どおり)。

## T4: 差分描画 (dirty-line) + 全文 join 撤廃 + 入力経路

### 変更の内容 (3 つ)

1. **差分描画**: 変更のあった行だけ描き直す。
   - `@dirty_lines` (行番号)、`@dirty_from` (この行以下すべて。行の挿入/削除で
     下がずれるとき)、`@dirty_status` (ステータス行だけ) を追加。構造が変わる
     もの (スクロール、メニュー、ダイアログ、レイアウト変更) は従来どおり
     `@need_redraw` の全面再描画に落ちる。**変換していない経路は全面再描画の
     まま動く**ので、退行の危険を最小にしてある。
   - draw_edit_area の行ループを `draw_edit_row` に切り出し、差分経路は
     dirty な行だけこれを呼ぶ。差分経路ではメニューバーと窓枠を再発行しない
     (行の描画では壊れない)。これが削減の大部分。
   - スクロール判定は ensure_cursor_visible に入れた (scroll_x/y が動いたら
     全面)。カーソル移動は「離れた行」と「来た行」を dirty にする。
2. **ハイライトを行単位に**: `@lines.join("\n")` → 全文 tokenize をやめ、
   描く行だけ `SyntaxHighlight.tokenize(line)` する。デバウンス
   (HL_DEBOUNCE_FRAMES) と全文マップ・行オフセット表も消えた。
3. **入力経路**: printable キーを `@input_buffer` に積んで別 Task
   (`Task.new` + `sleep_ms` の getch) が処理する形をやめ、**on_event で直接
   handle_key し、その場で差分描画する**。T2 の計測が示したとおり、遅延の
   主因は描画ではなく 33ms ポーリング 2 段だったため、これが無いと目標には
   届かない。副産物として Task.new と sleep_ms がエディタから消え、
   plan.md 6 章の Spinel 化障害 1 が解消した。

### 計測 (Linux sim 320x240, kernel=spinel, 70ms 間隔で 110 打鍵)

T1 時点と同じ条件・同じ入力パターン。

| 条件 | mean | max | p99 | 25ms 超 | draw mean |
|---|---|---|---|---|---|
| A 全画面/小 HL on (T1) | 29.1ms | 68.4ms | >50ms | 49/100 | 2.51ms |
| **A 全画面/小 HL on (T4)** | **2.50ms** | **12.7ms** | **<=15ms** | **0** | 2.50ms |
| B 全画面/大 10.9KB HL off (T1) | 28.4ms | 66.0ms | >50ms | 53/100 | 2.37ms |
| **B 全画面/大 HL off (T4)** | **1.36ms** | **6.6ms** | **<=5ms** | **0** | 1.35ms |
| D 窓/小 HL on (T1) | 29.0ms | 67.1ms | >50ms | 54/100 | 2.03ms |
| **D 窓/小 HL on (T4)** | **1.93ms** | **7.5ms** | **<=10ms** | **0** | 1.95ms |
| E 窓/大 HL off (T1) | 29.2ms | 69.8ms | >50ms | 52/100 | 2.71ms |
| **E 窓/大 HL off (T4)** | **1.30ms** | **4.3ms** | **<=5ms** | **0** | 1.29ms |
| C 全画面/大 10.9KB **HL on** (T1) | (エディタが停止) | - | - | - | - |
| **C 全画面/大 HL on (T4)** | **2.29ms** | **16.4ms** | **<=10ms** | **0** | 2.26ms |
| F 全画面/20.7KB HL on (T4) | 1.81ms | 8.8ms | <=10ms | 0 | 1.82ms |

- 目標 (**p99 < 33ms、25ms 超ゼロ**) を全条件で達成。平均で 10-20 倍速い。
- T1 の p99 欄が ">50ms" なのは、当時のログの p99 表示にバグ (ヒストグラムを
  クリアしてから p99 を計算していたので常に 55ms) があったため、記録した
  ヒストグラムから手計算した値。バグは修正済み。
- T1 で固まっていた「10KB + ハイライト ON」が動くようになった (条件 C)。
- この入力パターンは 1 行に 110 文字打つので途中から**横スクロールで毎回
  全面再描画になる**。つまり差分描画の効果はこの表より大きい。
  実際 draw_max (12-16ms) はその全面再描画のコストで、差分だけの打鍵は
  ヒストグラム最下段 (85-100/100 が 5ms 未満) に入っている。

画面側 (graphics-audio): 打鍵中 render_avg 25-35us、max 0.5ms。変化なし
(元々ボトルネックではない)。

### HL_AUTO_LIMIT_BYTES は撤廃した

判断材料: 行単位 tokenize になったのでハイライトのコストは**ファイルサイズに
依存しない** (描く行の数だけ)。10.9KB (719 行) で mean 2.3ms / 25ms 超ゼロ、
20.7KB (1349 行) で mean 1.8ms / 25ms 超ゼロ。よって 1KB の自動オフを残す
理由が無くなったので削除し、常に ON (メニューで手動 OFF 可) にした。

### 既知の差分 (v1 で許容)

- **複数行にまたがる構文の色**: heredoc、複数行文字列、`=begin` ブロックは
  行ごとに tokenize するため色が正しくないことがある。行単位化の対価。
- 全面再描画 (スクロール、メニュー開閉) では可視行ぶん tokenize するので
  ハイライト有効時の draw は T1 より重い (12-16ms、目標内)。行単位の
  キャッシュを入れれば下がるが、今は不要と判断した。

### 表示の正しさ (Linux sim, スクリーンショットで確認)

| ケース | 結果 |
|---|---|
| 打鍵 (差分描画) / Home で横スクロール復帰 | 残り・欠けなし |
| カーソル上下移動 (旧位置のカーソル消去) | OK |
| PageDown / 縦スクロール (20.7KB, HL on) | OK |
| Shift+Down の範囲選択 (4 行) / 矢印で選択解除 | OK (残りなし) |
| 検索ダイアログ → ヒットへジャンプ (Ln 363) | OK |
| デバッガペイン (Retro 下分割) + ガター + F9 の赤点 + その状態での打鍵 | OK |
| 窓モード / 全画面モードの両方 | OK |

- esp32 ビルド: S3 (NARYAv3) / P4 (TAB5) 両方成功。
- **ユーザ確認待ち**: 実機 (Retro / Modern) での体感と、実機での遅延計測
  (`edit_lat` 行がシリアルログに出る)。

### T4 で見つけた既存バグ (直したもの / 直していないもの)

- **直した**: 検索ダイアログの Enter / ESC が Linux sim で効かなかった。
  `ev[:keycode]` で 40/41 を見ていたが、sim では keycode が SDL keysym
  (13/27)。メニュー処理と同じく scancode 判定に変更 (実機では両方効いていた)。
- **直していない (別課題)**: **50KB のファイルを開くとエディタ VM が死ぬ**。
  20.7KB (1349 行) は正常、53.4KB (3421 行) は `file_selected` を受けた直後に
  例外ログも on_destroy も無くタスクが消える (アプリの 512KB プールの
  メモリ不足と思われる)。窓モード・従来の中継経路でも再現するので T4 とは
  無関係。ハイライトではなく**文書モデルの持ち方**の限界なので、
  plan.md 段階 4 (editor-gem 分割で文書モデルを Spinel/C 側へ) の
  対象とするのが筋。

## 完了報告 (P1 全体)

段階 1-3 (指示書 T1-T4) は完了。コミットはタスクごとに 4 本 + ツール/ドキュメント。

### ユーザ確認待ち

1. **実機の見た目と操作感** (Retro / Modern)。全画面エディタの表示、Ctrl+Tab の
   park/復帰、体感速度。Modern は remote desktop 経由で遠隔確認もできる。
2. **実機での遅延計測**。シリアルログの `edit_lat:` 行 (100 打鍵ごと) と、
   映像側の `loop: ... render_avg=..us` (WROVER) / `render: ... avg=..ms` (P4)。
   sim では画面合成が軽すぎて差が出ないので、窓 vs 全画面の suspend 効果は
   実機で見る価値がある。
3. 音・NTSC 出力には影響しない変更なので確認不要。

### 次段階 (plan.md 段階 4 以降) への引継ぎ

- **50KB ファイルでエディタ VM が死ぬ**のが今の実質的な上限 (T4 章末)。
  ハイライトではなく文書モデル (`@lines` の String 配列) がアプリの 512KB
  プールを食い切るため。段階 4 の editor-gem 分割 (文書モデルを Spinel/C へ)
  で解く筋の問題で、そこに着手する強い動機になっている。
- **Spinel 化の障害 1 は解消済み**: エディタから `Task.new` と `sleep_ms` が
  消え、入力は on_event 駆動になった (plan.md 6 章の 3 つのうち 1 つ)。
  残るのは FMRB::Debug の FFI と sp_io の VFS 接続。
- **段階 5 (640x360) の判断材料**: 描画コストの実測値がそろった。sim では
  全画面 27 行の全面再描画 (ハイライト有効) が 12-16ms、差分打鍵は 5ms 未満。
  行数が 45 行になると全面再描画は比例して伸びる (推定 20-27ms) ので、
  **640x360 に上げるなら全面再描画の頻度を下げる (行単位ハイライトの
  キャッシュ) 方が先**。差分描画だけなら余裕がある。
- sim を Modern 解像度で動かせるようになったので (T3)、段階 5 の UI 確認は
  実機を待たずに進められる。
