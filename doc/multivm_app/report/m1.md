# M1 完了報告: コメント埋め込み toml + /tmp RAM FS + テンプレート挿入

対象指示書: `doc/multivm_app/instruction_m1.md` (2026-08-10 発行)。
実施順は指示どおり T1 → T3 → T2。タスクごとに 1 コミット。

| タスク | 内容 | コミット |
|---|---|---|
| T1 | コメント埋め込み toml (多重 VM 計画 段階 3) | `f63beaf` |
| T3 | /tmp RAM FS (段階 2) | `b252d6d` |
| T2 | エディタのテンプレート挿入 | `5f69fb1` |

検証構成: Linux sim (FMRB_HW_TARGET=TAB5, 426x240)。標準構成
(kernel=spinel, editor=spinel) と、T2 は互換構成 (editor=mruby) でも確認。
esp32 は S3 (NARYAv3) / P4 (TAB5) ともビルド通過。実機動作はユーザ確認待ち。

---

## T1: コメント埋め込み toml

### 実装形

`main/app/fmrb_app_spawner.c` に `load_inline_toml()` を追加し、
`spawn_user_app()` のサイドカー読み込みの**後段**に置いた。

1. 対象は `.rb` のみ (拡張子で判定)。
2. `fmrb_hal_file_read` で先頭 512 バイトだけ読む。作業用の 512B x2 は
   `fmrb_sys_malloc` から取る (この関数はカーネルの深い呼び出しの先で
   動くので、既存コードと同じくスタックに置かない)。
3. 行単位で走査する。フェンス開始前は空行とコメント行だけ許し、
   **最初の非コメント行に当たったら探索を打ち切る**。
   `#---fmrb` 行 (以降は空白のみ) で本文に入り、`#---` 行で閉じる。
4. 本文の各行は行頭の `#` と続く空白 1 個を剥いで別バッファに積み、
   最後に **既存の `toml_parse()` にそのまま渡す**。新しい文法は無い。
5. 512 バイト窓の途中で行が切れた場合はそこで打ち切る (推測しない)。

`spawn_user_app()` 側は `config` の入手元が `.toml` か
コメントかを `config_from_comment` で覚えているだけで、**属性反映の
コードは 1 本のまま共用**している。したがって窓モード / large_memory /
task_stack_kb / resizable / fullscreen 等は `.toml` と同じキーが同じに効く。

例外は `app_screen_name` で、コメント側にあっても警告して無視する
(ランチャー表示系は `.toml` 限定という線引き。起動時スキャンが全 `.rb` を
開く事態を避けるため)。

### shell の `run` も直した (指示書に無い必要な追随)

`shell_commands.rb` の `detect_run_mode` は「`.toml` サイドカーがあれば
spawn、無ければ sandbox 実行」だった。このままではフェンス付きファイルが
**shell の VM 内で実行され、属性が一切効かない**。フェンスが開いている
`.rb` も spawn 扱いにする判定を足した (spawner と同じ 512 バイト窓・
同じ「最初の非コメント行で打ち切り」規則)。

### 事前確認の結果 (指示書の宿題)

**`.toml` 無しの `.app.rb` はランチャーに出ない**。
`system_desktop/launcher.rb` の `scan_app_dir` は `.toml` ファイルを
列挙して、そこから対応する script 名を導く作りで、`.rb` 単独は
走査対象に入らない (`launcher.rb:366` 付近)。

これは M1 の「ランチャー表示系メタデータは `.toml` 限定」という方針と
整合しているので**そのままにした**。フェンスだけのアプリの入口は
shell の `run`、ファイル管理、エディタの F5 になる。ランチャーにも
出したいならサイドカーを置く (フェンスと併用した場合はサイドカーが勝つ)。

### 確認したこと (sim)

| 確認項目 | 結果 | 画面 |
|---|---|---|
| フェンスの `default_window_mode = "background"` が効く (canvas 無し) | OK。`app_canvas: [fence_bg] Headless: no canvas allocated` | (ログ) |
| 同 `task_stack_kb = 32` が効く | OK。`Per-app task stack: 32 KB` | (ログ) |
| フェンスの `fullscreen` が効く | OK。426x240 全面・窓枠無し | `fs2.png` |
| `.toml` とフェンス両方 → `.toml` が勝つ | OK。180x90 の窓、名前も `.toml` 側 | `win.png` |
| 閉じ忘れフェンス → 警告 + 既定属性で起動 | OK。`Unterminated #---fmrb fence ...` の後 100x100 窓 | `bad.png` |
| 不正 toml 本文 → 既定属性で起動、spawn は失敗しない | OK (下記の注記あり) | (ログ) |

注記: `default_window_mode = fullscreen` (裸の値) と
`task_stack_kb = "not a number"` を入れた負例は、**tomlc99 が解析を
通してしまう**ため「解析エラーの警告」は出ない。ただし
`fmrb_toml_get_string` / `_get_int` が型不一致で既定値を返すので、
**結果は要求どおり「既定属性で起動」**になる。警告が出るのは
フェンスが閉じていない・本文にコメント以外が混ざる場合。

### 退行確認

| アプリ | 構成 | 結果 |
|---|---|---|
| `pub_demo` (.toml、窓) | 標準 | OK (窓と Publish ボタン表示) |
| `raycaster` (.toml、全画面) | 標準 | OK (全画面描画) |
| `rpg_demo` (.toml、全画面 + 資産読込) | 標準 | OK (マップ表示まで) |
| エディタ (組み込みテーブル経由の spawn) | 標準/互換 | OK |

---

## T3: /tmp RAM FS

### 構成

| 面 | 実体 |
|---|---|
| esp32 | `components/fmrb_hal/platform/esp32/fmrb_tmpfs_esp32.c` — POOL_ID_TMPFS を裏に持つ RAM FS を `esp_vfs_register("/tmp", ...)` で登録 |
| posix | `components/fmrb_hal/platform/posix/fmrb_tmpfs_posix.c` — `flash/tmp` に割り当て、起動時に空にする + 同じ容量上限を課す |
| 共通 | `components/fmrb_hal/fmrb_tmpfs.h` (`fmrb_tmpfs_init` / `fmrb_tmpfs_usage`)。両方とも `fmrb_hal_file_init()` から呼ぶ |

esp32 側で esp_vfs にしたのは、**この機械でファイルに触る経路が全部
libc 越しだから**。Ruby の File/IO と Dir、spawner、エディタ
(EditorCore の load/save)、transfer_file が、`/tmp` の存在を知らないまま
そのまま動く。`fmrb_hal_file_esp32.c` 側の変更は 3 行だけ:

- `s_path_aliases` に `{ "/tmp", "/tmp" }` (flash の下に畳まれないように)
- `/` の仮想 children に `"tmp"` を追加 (`ls /` に出る)
- `fmrb_hal_file_init` から `fmrb_tmpfs_init()` を呼ぶ

VFS 実装は open/close/read/write/lseek/fstat/stat/unlink/rename/
truncate/ftruncate/fsync/access/opendir/readdir(_r)/telldir/seekdir/
closedir を持つ。`mkdir`/`rmdir` は `ENOTSUP` を返す (下記)。

### プールサイズの根拠

```
FMRB_MEM_POOL_SIZE_TMPFS  = 512KB   (POOL_ID_TMPFS、PSRAM)
FMRB_TMPFS_CAPACITY_BYTES = 448KB   (ファイル内容として配る量)
```

- 専用プールにした理由は editor-core (P4) と同じ。アプリの mruby プール
  (500KB) に置くと**そのアプリの終了で消える**うえファイルサイズの上限に
  なり、SYSTEM 500KB は各種ドライバの共有財布なので大物を置けない。
- 512KB は **S3 基準**。S3 の PSRAM 残は 3.2MB で、うち 1MB は
  editor_doc が取っている。P4 は 20MB 以上空いているので、定数は
  ターゲット別に上げられるよう `FMRB_MEM_POOL_SIZE_TMPFS` 単独で持たせた。
- 配る量を 448KB に絞ったのは、割り当てヘッダと各ファイルの伸長余白
  (512B 単位) を吸わせるため。**書き込みの途中で確保に失敗するより、
  書く前に「満杯」を返すほうがアプリ側で受け止めやすい**。

### 意図的に付けた制約: /tmp は平ら (サブディレクトリ無し)

RAM FS 側を 1 段の名前表だけにしたので `/tmp/a/b.txt` は作れない。
posix 側にも同じ拒否を入れてある (`fmrb_tmpfs_path_too_deep`) ので、
**sim で通ったものが実機で通らない、という穴は開けていない**。

### 容量計上 (posix 側)

sim でも「満杯」を再現するため、`flash/tmp` 配下の書き込みバイト数を
数えている。ハンドルごとに「この文字列のうち既に計上済みのバイト数」を
持ち、書き込み位置がそれを超える分だけ課金する。truncate (`"w"` で開く)
と削除で返却する。**同一ファイルを 2 つのハンドルで同時に伸ばすと
二重計上になりうる**が、sim の近似としては許容した (実機側は RAM FS の
実配分なので正確)。

### 確認したこと (sim)

`flash/app/test/tmp_smoke.app.rb` (headless、フェンス付き) の実行ログ:

```
TMP: ok   exist / size / read / append size / append read / truncate size
TMP: ok   list has smoke1 / list has smoke2
TMP: ok   root has tmp / tmp is a dir
TMP: ok   renamed gone / renamed here / deleted
TMP: ok   flat mount            (サブディレクトリ作成が失敗すること)
TMP: refused at 454656 bytes (RuntimeError: write failed)
TMP: ok   full is reported      (アプリは生存して後始末まで走った)
```

454656B で拒否されたのは、直前に別ファイルが 3B 使っていたためで、
上限 458752B (=448KB) にちょうど一致する。

| 確認項目 | 結果 | 画面 |
|---|---|---|
| アプリ A が /tmp に書き、アプリ B が読む (pub/sub でパス通知) | OK。427B を受け渡し。`tmp_writer` "sent 1 (427B)" / `tmp_reader` "got 1 427B" | `handoff.png` (h5) |
| エディタで /tmp のファイルを保存 → 開き直す | OK (**Spinel エディタ**で実施 = Spinel 経路の確認) | `ed5.png` / `ed7.png` |
| 容量超過でエラー、アプリ生存 | OK (上記ログ) | (ログ) |
| 再起動で /tmp が空になる | OK。`/tmp ready: 448 KB capacity (1 stale entries cleared)` | (ログ) |
| `ls` 相当で /tmp の一覧が見える / `/` 直下に tmp が出る | OK | (ログ + `ed3.png` のファイル選択に `[tmp]`) |

---

## T2: エディタのテンプレート挿入

### 実装形

- テンプレートは `flash/lib/templates/*.rb` (実機では `/lib/templates`)。
  ファーム埋め込みにしていないので、ユーザが自分のものを置けば一覧に出る。
- エディタ側は `File` メニューに `Template` を追加 (項目は
  Open / Save / Save as / **Template** / Exit)。一覧は
  **既存のメニュー drop-down をそのまま使う** (`@active_menu = :template`)。
  上下キー・Enter・ESC・クリックの処理は元からあるものが効く。
- 挿入は `EditorCore.insert_multiline(@cy, @cx, body)`。貼り付けと同じ形で、
  選択があれば消してから入れ、戻り値から新カーソル位置を取る。
- 一覧はメニューを開くたびに読み直すので、**エディタを動かしたまま
  テンプレートを追加しても再起動なしで出る**。
- クラス名の置換は v1 ではやらない (指示どおり)。

### 同梱した 3 種

| ファイル | 内容 |
|---|---|
| `window.rb` | 窓アプリ。on_create / draw / on_event / on_update / on_destroy、`draw_window_frame` を毎回呼ぶ形 |
| `fullscreen.rb` | 全画面 + ゲームループ。`fullscreen_switchable` 付き、`on_resume` あり、矢印キーの例 (scancode を使う) |
| `worker.rb` | headless。処理を細切れにして `on_update` から戻る形 + /tmp へ結果 + publish で通知 |

**3 つとも末尾に起動トレーラ** (`begin / MyApp.new.start / rescue`) を
持ち、なぜ必要かをコメントで書いてある。

### 統合デモ (T1 + T3 + T2 が 1 本で成立することの確認)

手順 (sim、マウスとキーボードのみ):

1. デスクトップで `e` → エディタ起動
2. `File > Template > worker` → 雛形が挿入される (`t3.png`)
3. `File > Save as` → `/app/test/` へ `my_worker.app.rb` で保存
   (**サイドカー `.toml` は作らない**) (`t12.png`)
4. あらかじめ `worker_watch.app.rb` (topic "worker" の購読側) を起動しておく
5. エディタで **F5**

結果 (`t19.png`):

```
fmrb_default_apps: Loaded comment toml from /app/test/my_worker.app.rb   <- T1
app_canvas: [my_worker] Headless: no canvas allocated                    <- T1 (background)
my_worker: worker: wrote /tmp/worker_result.txt (total=29994)            <- T3
WorkerWatch: WATCH: /tmp/worker_result.txt -> total=29994                <- publish 通知
```

WorkerWatch の窓に "results: 1 / total=29994" が出る。

window / fullscreen の雛形も同じ手順で保存 → F5 し、
窓アプリは クリック数が増えること (`t25.png`)、
全画面アプリは全面で四角が動くこと (`t30.png`) を確認した。

### 両エンジンでの確認

| 構成 | Template 一覧表示 | 挿入 |
|---|---|---|
| 標準 (`FMRB_APP_ENGINE_EDITOR=spinel`) | OK (`t2.png`) | OK (`t3.png`) |
| 互換 (`FMRB_APP_ENGINE_EDITOR=mruby`) | OK (`m1.png`) | OK (`m2.png`) |

互換構成は `FMRB_APP_ENGINE_EDITOR=mruby rake build:linux` で確認
(コマンドライン env が .env より優先される)。Debug メニューが出ている
ことでエンジンの違いを確認できる。

---

## 後追い修正: F5 の起動場所制限を外した (2026-08-10)

M1 の後にユーザが踏んだ症状: **エディタで Template を挿入 → F5 →
保存先を聞かれて `/tmp` に保存 → 何も起動しない**。

原因は `run` 要求のパス許可リストが `/app/` と `/home/` だけだったこと
(`app_lifecycle.rb` の `run_path_allowed?`、エディタ側にも同じ判定の
`runnable_path?`)。/tmp が増えて保存ダイアログに `[tmp]` が出るように
なったので、この制限が表に出た。しかも**カーネルの `"spawn"` ハンドラ
(shell とランチャーが使う) には制限が無く**、同じファイルが
`run /tmp/foo.app.rb` では起動し、F5 では起動しないという食い違いに
なっていた。拒否の通知もステータス行右端の 2 秒バッジだけで気付けない。

判定を **「絶対パスならよい」** に変えた (ユーザ判断 2026-08-10)。

- 場所の制限は無くなり、/tmp でも SD でもどこでも F5 で起動する。
- 組み込み名 (`default/editor` 等) は `/` で始まらないので従来どおり
  弾かれる。これが元々の制限の趣旨だった (「デスクトップが spawn する
  ものを run で呼ばせない」)。
- 同じ述語を使う他の 2 箇所 (前回起動分を停止するか / Ctrl+Q で "stop" を
  送るか) は「そのアプリはファイル起動か」を訊いている。組み込みは
  `:path` を持たない (`""`) ので答えは以前と同じ。
  むしろ**副作用として直った**: 以前は /tmp のアプリを F5 で連打すると
  前の instance が停止されずスロットを食い潰していた。

確認 (sim): /tmp 保存 → F5 で起動 (`f1.png`)、再 F5 で前を停止して起動し直す
(`Run: stopping PID 5 first`)、Ctrl+Q で正常終了、/home と /app の F5 も従来
どおり。S3/P4 ビルド通過。

## 後追い修正 2: 全画面エディタからの RUN と、ファイル選択のダブルクリック

### 全画面から F5 したとき、全画面のまま裏でアプリが動く

ユーザ報告: 全画面エディタで RUN すると、アプリは起動しているのに
全画面が解除されず、アプリにフォーカスも移らない。

**再現条件は「同じファイルを 2 回目に F5 したとき」**。1 回目は
`after_spawn` が正しく park する (P2 の実装)。2 回目は前の instance を
止める経路 (`prev_stoppable`) に入るが、**全画面のエディタが他を全部
suspend しているので、止めたい相手も suspend 状態**にある。suspend 中の
アプリはメッセージ待ち行列を読まないので `"stop"` が届かず、終了通知も
来ず、`@pending_reload` の spawn が永久に発火しない。エディタは
"Running" と出したまま全画面に留まり、**前のアプリだけが裏で生き続ける** —
報告どおりの見え方になる。

修正: **要求元が全画面アプリなら、止める前に park する**
(`fmrb_kernel.rb` の run ハンドラ)。park は要求元の frame が覆っていた
もの (デスクトップと古い instance) を resume するので、古い方が `"stop"` を
受け取れるようになる。そしてこれは全画面からの RUN が行き着きたい状態
そのもの — 全画面を抜けてデスクトップに戻り、起動したアプリにフォーカスが
移る。アプリを閉じればエディタは全画面に復帰する (1 回目と同じ流れ)。

park できない場合 (switchable でない等) は古い instance を止めるのを諦め、
spawn の失敗として報告する。黙って固まるよりよい。

確認 (sim): 全画面エディタ → F5 (1 回目) → park してアプリ表示 →
Ctrl+Tab で戻る → **F5 (2 回目) → park してアプリ再起動・フォーカス**
(`h9.png`) → Ctrl+Q → エディタが全画面に復帰 (`h10.png`)。

### ファイル選択はダブルクリックで移動するようにした

ユーザ要望。従来はディレクトリに**シングルクリック**で入っていたため、
ランチャーで慣れたダブルクリックをすると、入った先の一覧で
同じ位置にある行 (たいてい `[..]`) をもう一度押したことになっていた。

`file_selector.rb` を「1 回で選択、2 回で実行」に変更 (判定はランチャーと
同じ `@counter` 差 < 5)。ディレクトリは移動、ファイルは open モードなら
確定。save モードでファイルを 1 回押すと従来どおり名前欄に入る。
移動直後は記憶した行番号を捨てるので、続けて押しても暴発しない。

併せて **`[..]` を 1 階層目から押すと壊れる既存不具合も直した**
(`"/app".split("/")` は `["", "app"]` で、pop 後の `join("/")` が `""` に
なり、HAL の解決で `flash/` に落ちて「中身は `/` なのに `[..]` がある
一覧」が出ていた)。書き換えた行そのものだったので同時に直している。

確認 (sim): 1 回クリックで反転のみ、2 回で `/app` に入る、`[..]` 2 回で
`/` に戻る (title が `Open: /`)、ファイル 2 回で開く (`h1`-`h6`)。

## 後追い修正 3: 全画面復帰の重ね順と、タスクバーのフォーカス表示

どちらも「フォーカスが動いたことを誰も知らせていない」系の抜け。

### Ctrl+Tab でアプリの下に全画面エディタが出る

F5 直後 (アプリが最前面・エディタは park 中) に Ctrl+Tab を押すと、
`cycle_front_app` が parked を選んで `unpark_fullscreen` に入る。この枝は
`_bring_to_front` を通らないので、**エディタは全画面に戻るのに重ね順は
アプリが上のまま**になり、全画面エディタの上にアプリの窓が浮く。
suspend は canvas を隠さないので、前にいたものは前にいたままになる。

`_bring_to_front` を呼んでいたのは、窓のクリック / `focus_app` /
全画面→窓トグル / Ctrl+Tab が**窓アプリ**に移るときの 4 箇所だけだった。
何度か押すと直って見えたのは、その後の操作がたまたま重ね順を書き直すため。

修正: `enter_fullscreen` の末尾で `_bring_to_front(pid)`。spawn 時の全画面・
unpark・入れ子の全部がここを通るので、1 箇所で揃う。

### タスクバーの白枠 (フォーカス表示) がほとんど出ない

`@taskbar_focused_pid` を代入していたのが**タスクバーのアイコンを
クリックしたときの 1 箇所だけ**だった。F5 での起動・Ctrl+Tab・窓の
クリック・アプリ終了でフォーカスが動いてもデスクトップは知らされず、
白枠は出ないまま (または前にクリックしたアイコンに付いたまま) になる。

修正: カーネルの `tick_process` から `tick_focus_notify` を呼び、
`@hid_target_pid` が前回通知値と変わっていたらデスクトップへ
`{"cmd" => "focus_changed", "pid" => N}` を 1 通送る。デスクトップは
`on_control` で受けて `@taskbar_focused_pid` を更新し再描画する。

**各フォーカス移動地点ではなく tick から送る**のが要点。フォーカスは
spawn / Ctrl+Tab / park / unpark / クリック / 終了 と 10 箇所近くで動き、
1 箇所足すたびに取りこぼしが残る。tick の比較 1 回なら漏れようがない。
送信に失敗したときは記録を進めないので次の tick で送り直す。
C API の追加は不要 (`@hid_target_pid` は既にカーネルの Ruby が持っている)。

確認 (sim): エディタ起動直後に白枠が付く (`tb3.png`)、park すると外れる
(desktop にフォーカスが移るため、`tb6.png`)、アプリの窓をクリックすると
そちらへ移る (`tb7.png`)。全画面 → F5 → Ctrl+Tab で、アプリの窓が
上に残らず全画面エディタだけになる (`k7.png`)。

### タスクバーのアイコンが 1 秒遅れて出る

上の確認中に見つけた。`update_taskbar_apps` はデスクトップの約 1 秒周期の
自前ポーリングでしか走らないので、窓が出てからアイコンが出るまで最大 1 秒
空いていた (`FmrbApp.ps` が重いので間引くのは意図的)。

修正は 2 つ:

1. カーネルの `after_spawn` と `cleanup_terminated_app` から
   `{"cmd" => "apps_changed"}` をデスクトップへ送る。プロセスの増減は
   この 2 箇所に集約されているので、フォーカスと違って tick 比較にする
   必要はない。デスクトップは受けたら `update_taskbar_apps` +
   再描画 (`ps_gen` の門は残してあるので余計な通知は counter 読み 1 回)。
2. **一覧の対象に state=INIT を足す**。`after_spawn` が走る時点で新タスクは
   まだ INIT で、RUNNING/SUSPENDED しか拾っていなかったため、1 の通知で
   作り直しても新アプリが入らず結局ポーリング待ちになっていた。

実測 (sim、キー押下からの経過): 修正前は窓 0.1 秒・アイコン 0.5 秒。
修正後は**同一フレームで 0.09 秒**。終了時のアイコン消滅も 0.09 秒。
shell の `run` で起動した場合も同じ。

### 起動ロゴの演出が壊れた (上の修正の巻き戻し)

ユーザ報告: ロゴが少しずつ見えてくる演出のはずが、最初からロゴと
タスクバーが出てしまう。

原因は上の 2 つの通知で、**起動アニメーション中に `draw_foreground` を
呼んでいた**こと。デスクトップは起動時に前面 canvas を黒で塗り潰し、
そこを iris 状に開いて背面のロゴを見せる作りで、前面の実描画は
`finish_boot_animation` が 1 回だけ行う。カーネルは boot 直後に HID target を
デスクトップへ向けるので `focus_changed` が真っ先に届き、その場で前面を
描いてしまってメニューバーとタスクバーが黒幕の代わりに出ていた。

修正: 両ハンドラの描画を `@boot_anim_state == :done` で囲む。一覧と
フォーカスの記録自体は続けるので、演出が終わったときの
`finish_boot_animation` の描画で正しい状態が出る。

確認 (sim): 黒 → 中央に小さく開く → 菱形に広がる → ロゴ全体 → 壁紙と
メニューバー、の順に戻った (`bootstrip.png`)。アイコン即時表示も
0.09 秒のまま。

---

## ユーザ確認待ち

- **実機 (S3 / P4) での /tmp**: esp_vfs 版 RAM FS は sim では動かない
  経路なので、実機で `tmp_smoke.app.rb` を 1 回流してほしい
  (期待: すべて `ok`、`refused at` が 448KB 付近)。
- **実機でのコメント toml 起動**: `fence_bg` / `fence_fs` を shell の
  `run` で起動。
- **実機でのテンプレート挿入**: `/lib/templates` が littlefs に載って
  いること (flash イメージに `flash/lib/templates/` が入る) と、
  File > Template の表示。
- 操作感 (メニューの位置、雛形の内容が実際に書き始めやすいか)。

## 引継ぎ (段階 1 / 段階 4 へ)

- **worker 雛形が段階 1 (オフロード最小デモ) の入口**になっている。
  本格版で埋めるべき穴は計画 2.3 のとおりで、今回の検証中に 2 つ実際に
  踏んだ:
  - **spawn 時に HID が子へ移る**: headless の worker を起動しただけで
    キーボードが親から離れる。worker が終了した後も親 (shell) には
    戻らずデスクトップへ行った。段階 4 の前に `after_spawn` の
    `_set_hid_target` を background では飛ばす改修が要る (計画 3.5)。
  - **ユーザアプリ 3 枠の窮屈さ**: エディタ + 購読側 + worker で満枠に
    なり、統合デモでは shell を閉じる必要があった。枠が埋まったときの
    メッセージ ("No free app slot. Close a running app and try again.")
    自体は正しく出る。
- **Request/Response (段階 4)**: 今回の worker 雛形は「書いてから
  publish で通知」の一方向。req_id と待ち合わせを載せる土台として、
  大きなデータをパスで渡す作法 (計画 3.3 の後半) は /tmp で成立済み。
- `.bas` / `.lua` / `.py` のコメント toml は未対応 (コメント記号が違う)。
  同じフェンス解析器に「行頭コメント記号」を引数で渡せば拡張できる形に
  なっている。
- テンプレートのクラス名置換 (雛形の `MyApp` を入力名にする) は将来案。
  挿入後に `Search` で置換する導線でも足りるかもしれないので、要望が
  出てから判断する。

## 画面ファイル

検証中のスクリーンショットはセッションの作業領域に置いた (リポジトリには
入れていない)。本文中の `fs2.png` / `handoff.png` 等はその中の名前。
