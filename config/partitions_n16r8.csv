# ESP32-S3 N16R8 (Family mruby Retro / Narya v3) - 16MB flash
#
# factory was 3M until the networking gems (picoruby-socket + mbedTLS) pushed
# the image past it; 4M restores ~1MB of headroom. The flash is 16MB and this
# layout uses ~9MB, so growing the app partition costs nothing -- but a
# partition-table change cannot ship as an app-only update, it needs a full
# flash (bootloader + table + app + storage).
# Name,   Type, SubType,  Offset,  Size, Flags
nvs,      data, nvs,      0x9000,  24K,
phy_init, data, phy,             ,  4K,
factory,  app,  factory,         ,  4M,
storage,  data, littlefs,        ,  5M,
