app_handle_name = "fps_bench_region"
app_screen_name = "BenchRegion"
app_screen_name_ja = "FPS計測(分割)"
default_window_mode = "window"
default_window_width = 280
default_window_height = 200
default_window_pos_x = 20
default_window_pos_y = 20
# rounded_corners default = 1 (true). Canvas is created use_transparent=true,
# but compositor sub-rect splits the canvas into one big opaque interior and
# 4 small transparent corners via set_composite_regions.
